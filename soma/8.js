const black = document.getElementById('black');
const blackLabel = document.getElementById('blacklabel');

const changeBlack = () => {
    if (blackLabel.className === 'black') {
        blackLabel.className = '';
    } else {
        blackLabel.className = 'black';
    }
};

const red = document.getElementById('red');
const redLabel = document.getElementById('redlabel');

const changeRed = () => {
    if (redLabel.className === 'red') {
        redLabel.className = '';
    } else {
        redLabel.className = 'red';
    }
};

const green = document.getElementById('green');
const greenLabel = document.getElementById('greenlabel');

const changeGreen = () => {
    if (greenLabel.className === 'green') {
        greenLabel.className = '';
    } else {
        greenLabel.className = 'green';
    }
};

const blue = document.getElementById('blue');
const blueLabel = document.getElementById('bluelabel');

const changeBlue = () => {
    if (blueLabel.className === 'blue') {
        blueLabel.className = '';
    } else {
        blueLabel.className = 'blue';
    }
};

const white = document.getElementById('white');
const whiteLabel = document.getElementById('whitelabel');

const changeWhite = () => {
    if (whiteLabel.className === 'white') {
        whiteLabel.className = '';
    } else {
        whiteLabel.className = 'white';
    }
};
